#ifndef SOLVE_UTILS_HPP
#define SOLVE_UTILS_HPP

#include <cstddef>

void hex_to_bytes(const char* src, unsigned char* target);
void print_bytes_hex(const unsigned char* bytes, size_t len);

#endif /* end of include guard: SOLVE_UTILS_HPP */
